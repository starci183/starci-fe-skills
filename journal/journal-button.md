# Journal — button

Design knowledge for the **button** block in StarCi. FAB, rating, input-like buttons.

**StarCi blocks in this family:** `FloatingActionButton`, `RatingBar`, `InputButtonLike` (blocks/buttons)
**Base styling rule (element layer):** see `../rules/` — this journal is the *design* layer above it.

> Starts as a stub. It GROWS: each time a button is designed and thầy gives feedback, add a dated entry
> below (or stage in `drafts/` then `/starci-fe-update-mindset` folds it in).

## House decisions (newest first)
_(empty — add: what was chosen · why · which page · ref anchor)_

## Gotchas
_(empty)_
