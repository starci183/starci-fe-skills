# Journal — page-header

Design knowledge for the **page-header** block in StarCi. Page container + header + sticky bottom bar.

**StarCi blocks in this family:** `PageHeader`, `PageContainer`, `StickyBottomBar`, `ResizableRail` (blocks/layout)
**Base styling rule (element layer):** see `../rules/` — this journal is the *design* layer above it.

> Starts as a stub. It GROWS: each time a page-header is designed and thầy gives feedback, add a dated entry
> below (or stage in `drafts/` then `/starci-fe-update-mindset` folds it in).

## House decisions (newest first)
_(empty — add: what was chosen · why · which page · ref anchor)_

## Gotchas
_(empty)_
