# Journal — media

Design knowledge for the **media** block in StarCi. Cover images / media surfaces.

**StarCi blocks in this family:** `CoverImage`, `MediaCard` (blocks/media, blocks/cards)
**Base styling rule (element layer):** see `../rules/` — this journal is the *design* layer above it.

> Starts as a stub. It GROWS: each time a media is designed and thầy gives feedback, add a dated entry
> below (or stage in `drafts/` then `/starci-fe-update-mindset` folds it in).

## House decisions (newest first)
_(empty — add: what was chosen · why · which page · ref anchor)_

## Gotchas
_(empty)_
