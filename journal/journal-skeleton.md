# Journal — skeleton

Design knowledge for the **skeleton** block in StarCi. Skeleton loaders (mirror layout).

**StarCi blocks in this family:** `Skeleton`, `CourseCardSkeleton` (blocks/skeleton, blocks/cards)
**Base styling rule (element layer):** see `../rules/` — this journal is the *design* layer above it.

> Starts as a stub. It GROWS: each time a skeleton is designed and thầy gives feedback, add a dated entry
> below (or stage in `drafts/` then `/starci-fe-update-mindset` folds it in).

## House decisions (newest first)
_(empty — add: what was chosen · why · which page · ref anchor)_

## Gotchas
_(empty)_
