# Journal — accordion

Design knowledge for the **accordion** block in StarCi. Collapsible/disclosure rows (content map, FAQ).

**StarCi blocks in this family:** `ContentMapRow` (blocks/navigation) + HeroUI `Accordion`
**Base styling rule (element layer):** see `../rules/` — this journal is the *design* layer above it.

> Starts as a stub. It GROWS: each time a accordion is designed and thầy gives feedback, add a dated entry
> below — the journal updates itself at the END of the task (no manual command); optionally stage in `drafts/` mid-work.

## House decisions (newest first)
_(empty — add: what was chosen · why · which page · ref anchor)_

## Gotchas
_(empty)_
