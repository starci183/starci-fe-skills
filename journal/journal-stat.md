# Journal — stat

Design knowledge for the **stat** block in StarCi. Metrics, progress, charts, roadmaps.

**StarCi blocks in this family:** `MetricCard`, `ProgressMeter`, `StatPair`, `SegmentBar`, `LanguageDonut`, `MilestoneRoadmap`, `TopicMasteryGrid` (blocks/stats)
**Base styling rule (element layer):** see `../rules/` — this journal is the *design* layer above it.

> Starts as a stub. It GROWS: each time a stat is designed and thầy gives feedback, add a dated entry
> below (or stage in `drafts/` then `/starci-fe-update-mindset` folds it in).

## House decisions (newest first)
_(empty — add: what was chosen · why · which page · ref anchor)_

## Gotchas
_(empty)_
