# Journal — sidebar

Design knowledge for the **sidebar** block in StarCi. Collapsible nav rail + its groups/items.

**StarCi blocks in this family:** `CollapsibleSidebar`, `SidebarNavGroup`, `SidebarNavItem` (blocks/navigation)
**Base styling rule (element layer):** see `../rules/` — this journal is the *design* layer above it.

> Starts as a stub. It GROWS: each time a sidebar is designed and thầy gives feedback, add a dated entry
> below (or stage in `drafts/` then `/starci-fe-update-mindset` folds it in).

## House decisions (newest first)
_(empty — add: what was chosen · why · which page · ref anchor)_

## Gotchas
_(empty)_
