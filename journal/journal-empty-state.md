# Journal — empty-state

Design knowledge for the **empty-state** block in StarCi. Loading/empty/error states.

**StarCi blocks in this family:** `AsyncContent`, `EmptyContent`, `ErrorContent`, `EmptyState`, `ErrorState` (blocks/async, blocks/feedback)
**Base styling rule (element layer):** see `../rules/` — this journal is the *design* layer above it.

> Starts as a stub. It GROWS: each time a empty-state is designed and thầy gives feedback, add a dated entry
> below (or stage in `drafts/` then `/starci-fe-update-mindset` folds it in).

## House decisions (newest first)
_(empty — add: what was chosen · why · which page · ref anchor)_

## Gotchas
_(empty)_
