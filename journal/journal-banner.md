# Journal — banner

Design knowledge for the **banner** block in StarCi. Marketing heroes, pitch, manifesto, ladders.

**StarCi blocks in this family:** `HeroBanner`, `PitchCard`, `SectionHeading`, `TrackLadder`, `FounderManifesto` (blocks/marketing)
**Base styling rule (element layer):** see `../rules/` — this journal is the *design* layer above it.

> Starts as a stub. It GROWS: each time a banner is designed and thầy gives feedback, add a dated entry
> below — the journal updates itself at the END of the task (no manual command); optionally stage in `drafts/` mid-work.

## House decisions (newest first)
_(empty — add: what was chosen · why · which page · ref anchor)_

## Gotchas
_(empty)_
